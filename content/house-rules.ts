/**
 * Edit this file with the real house rules text. `rulesVersion` is stored
 * on every acceptance record (rules_text_snapshot + rules_version) — bump
 * it whenever the text changes so old acceptances stay tied to what the
 * guest actually agreed to.
 */
export const rulesVersion = "2026-08-27-v1";

export const houseRulesText = `
Replace this with the real BaseKC house rules text. It is rendered in full
on the portal page (not collapsed) and the exact text shown is captured
verbatim into the acceptance record for dispute documentation.
`.trim();
