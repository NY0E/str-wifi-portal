# Supabase (server-side only — service role key bypasses RLS, never expose to the client)
SUPABASE_URL=https://qpbiejkshgobfxxrvryf.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Hospitable Public API v2
HOSPITABLE_API_BASE_URL=https://public.api.hospitable.com/v2
HOSPITABLE_API_KEY=

# UniFi — Site Manager API key, proxied to the console via unifi.ui.com
UNIFI_API_BASE_URL=https://unifi.ui.com/proxy/network
UNIFI_API_KEY=
UNIFI_SITE_ID=

# Admin lookup page (/admin) — HTTP Basic Auth
ADMIN_USERNAME=
ADMIN_PASSWORD=
